package etapa2.handlers

object FormatRunner {
    fun format(ast: Any, configPath: String?): String {
        // Usá tu formatter real; si no lo tenés, dejá el stub
        return /* Formatter().format(ast, configPath) */ ast.toString()
    }
}